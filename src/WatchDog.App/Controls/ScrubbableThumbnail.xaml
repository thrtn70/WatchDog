<UserControl x:Class="WatchDog.App.Controls.ScrubbableThumbnail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ClipToBounds="True">

    <Grid x:Name="RootGrid">
        <!-- Static thumbnail (default state) -->
        <Image x:Name="StaticImage" Stretch="UniformToFill"
               RenderOptions.BitmapScalingMode="HighQuality"/>

        <!-- Scrub frame (shown during hover when strip is loaded) -->
        <Image x:Name="ScrubImage" Stretch="UniformToFill"
               RenderOptions.BitmapScalingMode="HighQuality"
               Visibility="Collapsed"/>

        <!-- Scrub position indicator — thin bar at bottom -->
        <Border x:Name="ScrubBar" VerticalAlignment="Bottom" Height="2"
                HorizontalAlignment="Left" Width="0"
                Visibility="Collapsed" IsHitTestVisible="False">
            <Border.Background>
                <StaticResource ResourceKey="AccentBrush"/>
            </Border.Background>
        </Border>

        <!-- Loading shimmer (while generating strip on first hover) -->
        <Border x:Name="LoadingOverlay" Visibility="Collapsed" IsHitTestVisible="False"
                Background="{StaticResource ShimmerOverlayBrush}">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <Trigger Property="Visibility" Value="Visible">
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="ShimmerStory">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="Opacity"
                                            From="0" To="1" Duration="0:0:0.6"
                                            AutoReverse="True" RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="ShimmerStory"/>
                            </Trigger.ExitActions>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>
    </Grid>
</UserControl>
